import {test} from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';test('site includes navigation, architecture and honest deployment status',async()=>{const html=await readFile('web/index.html','utf8');assert.match(html,/id="architecture"/);assert.match(html,/does not prove Azure resources are running/);assert.match(html,/lang="en"/);assert.match(html,/style.css/);});